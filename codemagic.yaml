workflows:
  ios-workflow:
    name: iOS Build
    environment:
      flutter: stable
    scripts:
      - name: Build iOS
        script: |
          flutter build ios --no-codesign
    artifacts:
      - build/ios/iphoneos/*.app
      - build/ios/**/*.ipa